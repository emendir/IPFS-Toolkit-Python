
FileTransmission:
  abortion
  test blocksize upgrades

DataTransmission: terminate properly on error (test it in conv properly, see if more needs to be done for FileTransission)

include encryption parameters in transmit_data?
add more realistic encryption scenario to Encryption example


# Documentation
ipfs_cli: docs


# Tests
verify docker container is new on build (cause we've disabled verbose output on docker build)