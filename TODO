Conversation:
  Sendfile function

Transmitter:
  Progress function
  recheck if custom protocol can be replaced with TCP, keeping limitlessness

FileTransmission:
  abortion
  
  
Document new Conversation functionality and publish it